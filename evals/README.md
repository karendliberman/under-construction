# Evals

Python fixtures and scripts. **Does not deploy** — neither Dockerfile copies this
directory.

Arrives in Phase 4.6: N saved fact patterns per cause of action, a script to run
them, and a diff against the last run. Expect variance — the agent is
non-deterministic by design, so this is a "did output get worse" check rather
than an exact-match test.
